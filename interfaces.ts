export interface Task {
    "id": number;
    "task": string;
    "complete": boolean;
}

export interface TaskNotes {
    "id": number;
    "task": string;
    "complete": boolean;
}

export interface ToDoFormInterface {
    
}